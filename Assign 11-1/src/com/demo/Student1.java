package com.demo;

public class Student1 {
	//Student student = new Student(); // this calls a private constructor(instead of default) in Student class
    
	// now to resrict object creation from another class
	// have to have a constructor with mis-matiching argument in constructor
	               //or
	//need to have a private constructor
}
